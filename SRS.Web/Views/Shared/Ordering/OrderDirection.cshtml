@using SRS.Web.Models.Shared
@model OrderDirectionViewModel
@if (!Model.OrderType.HasValue || Model.OrderBy == Model.OrderType)
{
    <i class="bi @(Model.Desc ? "bi-sort-down" : "bi-sort-up")"></i>
}