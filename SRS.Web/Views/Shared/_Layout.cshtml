@using Microsoft.AspNet.Identity
@using SRS.Domain.Enums
@using SRS.Services.Models.Constants

<!DOCTYPE html>
<html>
<head>
    <link rel="shortcut icon" href="https://lnu.edu.ua/favicon.ico">
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    @RenderSection("styles", required: false)
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.0/font/bootstrap-icons.css">
</head>
<body>
    <div class="navbar navbar-srs navbar-fixed-top">
        <div class="container-fluid header">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <div class="navbar-brand text-white">@Html.ActionLink("ЛНУ ім. Івана Франка", "Index", "Home")</div>
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    @if (User.Identity.IsAuthenticated)
                    {
                        <li>@Html.ActionLink("Публікації", "Index", "Publications", new { UserId = User.Identity.GetUserId() }, null)</li>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                                Звіти <span class="caret"></span>
                            </a>
                            <ul class="dropdown-menu">
                                @if (User.IsInRole(RoleNames.Worker))
                                {
                                    <li>@Html.ActionLink("Поточний звіт працівника", "Index", "Report")</li>
                                }
                                @if (User.IsInRole(RoleNames.CathedraAdmin))
                                {
                                    <li>@Html.ActionLink("Поточний звіт кафедри", "InDevelopment", "Home")</li>
                                }
                                @if (User.IsInRole(RoleNames.DeaneryAdmin))
                                {
                                    <li>@Html.ActionLink("Поточний звіт факультету", "InDevelopment", "Home")</li>
                                }
                                @if (User.IsInRole(RoleNames.RectorateAdmin))
                                {
                                    <li>@Html.ActionLink("Поточний звіт університету", "InDevelopment", "Home")</li>
                                }
                                <li>@Html.ActionLink("Архів індивідуальних звітів", "Index", "ReportList")</li>
                                @if (User.IsInRole(RoleNames.Superadmin)
                                    || User.IsInRole(RoleNames.RectorateAdmin)
                                    || User.IsInRole(RoleNames.DeaneryAdmin)
                                    || User.IsInRole(RoleNames.CathedraAdmin))
                                {
                                    <li>@Html.ActionLink("Архів кафедральні звітів", "InDevelopment", "Home")</li>
                                }
                                @if (User.IsInRole(RoleNames.Superadmin)
                                    || User.IsInRole(RoleNames.RectorateAdmin)
                                    || User.IsInRole(RoleNames.DeaneryAdmin))
                                {
                                    <li>@Html.ActionLink("Архів факультетські звітів", "InDevelopment", "Home")</li>
                                }
                                @if (User.IsInRole(RoleNames.Superadmin)
                                    || User.IsInRole(RoleNames.RectorateAdmin))
                                {
                                    <li>@Html.ActionLink("Архів університетські звітів", "InDevelopment", "Home")</li>
                                }
                            </ul>
                        </li>
                        if (User.IsInRole(RoleNames.Superadmin)
                                    || User.IsInRole(RoleNames.RectorateAdmin)
                                    || User.IsInRole(RoleNames.DeaneryAdmin)
                                    || User.IsInRole(RoleNames.CathedraAdmin))
                        {
                            <li class="dropdown">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                                    Керування <span class="caret"></span>
                                </a>
                                <ul class="dropdown-menu">
                                    <li>@Html.ActionLink("Працівники", "Index", "UsersManagement")</li>
                                    <li>@Html.ActionLink("Зовнішні сумісники", "Index", "ExternalPartTimeEmployee")</li>
                                    @if (User.IsInRole(RoleNames.Superadmin) || User.IsInRole(RoleNames.RectorateAdmin))
                                    {
                                        <li>@Html.ActionLink("Журнали", "Index", "Journals")</li>
                                        <li>@Html.ActionLink("Тематичні плани", "Index", "ThemeOfScientificWorks", new { IsActive = true }, null)</li>
                                        <li>@Html.ActionLink("Наукові ступені", "Index", "Degrees")</li>
                                        <li>@Html.ActionLink("Факультети", "Index", "Faculties")</li>
                                        <li>@Html.ActionLink("Кафедри", "Index", "Cathedras")</li>
                                        <li>@Html.ActionLink("Почесні звання", "Index", "HonoraryTitles")</li>
                                        <li>@Html.ActionLink("Посади", "Index", "Positions")</li>
                                        <li>@Html.ActionLink("Вчені звання", "Index", "AcademicStatus")</li>
                                    }
                                </ul>
                            </li>
                        }
                    }
                </ul>
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
    </div>
    <div class="container-fluid footer-container">
        <hr />
        <div class="col-xs-12">
            <footer>
                <p class="text-center">&copy; @DateTime.Now.Year - ЛНУ ім. Івана Франка</p>
            </footer>
        </div>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
